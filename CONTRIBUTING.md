# مشارکت در T-AIR

اگر عددی در این دیتابیس اشتباه است، خوشحال می‌شوم اصلاحش کنید. مهم‌ترین چیزی
که این پروژه لازم دارد دقت است، نه قابلیت تازه.

## اگر فقط می‌خواهید یک اشتباه را گزارش کنید

یک [issue](../../issues) باز کنید و سه چیز بنویسید: شناسه‌ی رکورد (همان که در
آدرس بعد از `#/` می‌آید)، فیلدی که غلط است، و منبعی که عدد درست را می‌گوید.
همین کافی است — لازم نیست کد بزنید.

## اگر می‌خواهید خودتان اصلاح کنید

**جدول‌های داده را مستقیم ویرایش نکنید.** هر اصلاحی به‌صورت یک لایه‌ی جدا ثبت
می‌شود تا معلوم بماند چه چیزی، کِی و چرا عوض شده. این ساختار عمدی است: بدون آن،
شش ماه بعد هیچ‌کس نمی‌داند عدد فعلی از کجا آمده.

یک فایل تازه در `scripts/fixes/` بسازید (یا به فایل مناسبی که هست اضافه کنید):

```python
# -*- coding: utf-8 -*-
"""چه چیزی را بررسی کردید و بر چه اساسی."""

FIXES = {
    "boeing-737-800": {"rangeKm": 5765},   # was 5436 — Boeing 737 page, 2026-08
}
```

سه ساختار دیگر هم می‌توانید صادر کنید:

- `FAMILY_COUNT = ["id", ...]` — وقتی تیراژ منتشرشده مربوط به کل خانواده است نه
  این یک نسخه. عدد سر جایش می‌ماند و رابط کنارش می‌نویسد «کل خانواده».
- `VERIFIED = ["id", ...]` — رکوردهایی که فیلد به فیلد با منبع مقایسه کرده‌اید.
  در رابط علامت می‌خورند، پس فقط وقتی بنویسید که واقعاً همه‌ی فیلدها را دیده‌اید.
- `UNRESOLVED = ["...", ...]` — چیزهایی که به نتیجه نرسیدید. این فهرست ارزش دارد؛
  یک سؤال باز ثبت‌شده بهتر از یک حدس است.

## قاعده‌ی اصلاح

عددها بین منابع فرق می‌کنند و بیشتر این تفاوت‌ها خطا نیستند. پیش از اصلاح:

- مطمئن شوید رقم منبع برای **همان نسخه‌ای** است که رکورد نام برده. بیشترین خطای
  این پروژه از اینجا آمده: عدد خانواده روی رکورد یک نسخه.
- قراردادها را رعایت کنید: `speedKmh` برای غیرنظامی سرعت سفر و برای نظامی سرعت
  بیشینه است؛ `spanM` برای بالگرد قطر روتور اصلی است؛ `ceilingM` سقف سرویس است
  نه سقف شناوری؛ `crew` برای پهپاد صفر است و این خالی نیست.
- واحدها متریک‌اند: کیلومتر، کیلومتر بر ساعت، کیلوگرم، متر.
- اختلاف کمتر از حدود ۵٪ معمولاً اختلاف گردکردن یا پیکربندی است، نه غلط.

## پیش از هر کامیت

```bash
bash scripts/check.sh
```

داده را از نو می‌سازد، ۳۹ بررسی دیتابیس و ۲۴ بررسی رابط را اجرا می‌کند، و
نسخه‌ی تک‌فایلی و صفحه‌های ایستا را به‌روز می‌کند. همین‌ها با هر پوش روی GitHub
هم اجرا می‌شوند، پس اگر محلی سبز نشود آنجا هم نمی‌شود.

اگر بررسی‌ای شکست خورد و فکر می‌کنید خودِ بررسی اشتباه است، همان را در issue
بنویسید — چند بار پیش آمده که تست درست بوده و داده غلط، و یکی دو بار برعکس.

## افزودن رکورد تازه

یک ردیف به فایل مناسب در `scripts/parts/` اضافه کنید (ترتیب ستون‌ها بالای هر
فایل نوشته شده)، و ترجمه‌ی انگلیسی توضیح و نقش و تسلیحات را در `scripts/parts/en_*.py`
بگذارید. `scripts/test_build.py` اگر چیزی جا افتاده باشد می‌گوید.

## چیزی که پذیرفته نمی‌شود

- عکس با مجوز غیرآزاد. `scripts/fetch_photos.py` فقط فایل‌های ویکی‌مدیا کامانز
  با مجوز باز را برمی‌دارد و این عمدی است.
- عددی بدون منبع، حتی اگر مطمئن باشید. «می‌دانم» در کامیت قابل بازبینی نیست.
